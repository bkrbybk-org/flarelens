node_modules
.wrangler/
.dev.vars
# Real deployment config: account, zone and Access ids. wrangler.jsonc ships placeholders.
wrangler.local.jsonc
.env
.env.*
dist/
*.log
.DS_Store
worker-configuration.d.ts

# Local Wrangler / Miniflare caches (account IDs, request metadata)
**/.cache/wrangler/
**/.mf/

# CodeSight local scanner config (machine-specific paths)
.codesight/

# Vite build output + TS project build info
web/dist/
.tsbuild/
*.tsbuildinfo

# Swagger UI assets copied from node_modules by scripts/copy-swagger-ui.mjs at build time
web/public/docs/
